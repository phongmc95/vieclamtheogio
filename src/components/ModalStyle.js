import React from 'react';
import {StyleSheet, Text, View} from 'react-native';
import Modal from '@react-na';
const ModalStyle = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default ModalStyle;

const styles = StyleSheet.create({});
