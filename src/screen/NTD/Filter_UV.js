import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Filter_UV = () => {
  return (
    <View>
      <Text />
    </View>
  );
};

export default Filter_UV;

const styles = StyleSheet.create({});
